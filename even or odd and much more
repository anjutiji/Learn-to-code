#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n;
    scanf("%d", &n);
    if (n%2==0)
    {
        printf("Even\n");
        printf("%d", n*n);
    }
    else
    {
        printf("Odd\n");
        printf("%d", n*n*n);
    }
    return 0;
}
