#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int first,second,*p,*q,sum;
    scanf("%d%d", &first, &second);
    p=&first;
    q=&second;
    sum=*p+*q;
    printf("Sum is %d\n", sum);
    return 0;
}
  
